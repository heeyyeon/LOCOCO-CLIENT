{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo run build --filter=@lococo/web",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
